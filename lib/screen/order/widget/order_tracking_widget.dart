import 'package:flutter/material.dart';

class OrderTrackingWidget extends StatelessWidget {
  const OrderTrackingWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('have to fill');
  }
}
